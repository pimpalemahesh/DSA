// AUTO-GENERATED by scripts/gen_code_data.py — do not edit by hand.
window.CODE_DATA = {"files": [{"path": "arrays/sliding_window.cpp", "folder": "arrays", "folderLabel": "Arrays", "name": "sliding_window.cpp", "desc": "Sliding Window — Phase 2 (Arrays & Strings)", "code": "// Sliding Window — Phase 2 (Arrays & Strings)\n// Compile: g++ -std=c++17 -Wall sliding_window.cpp -o /tmp/out && /tmp/out\n#include <bits/stdc++.h>\nusing namespace std;\n\n// Max sum of any contiguous subarray of length k. Time: O(n), Space: O(1).\nlong long maxSumWindow(const vector<int>& a, int k) {\n    // TODO: build the first window sum, then slide: add a[i], remove a[i-k].\n    (void)a; (void)k;\n    return 0;\n}\n\n// Length of the longest substring without repeating characters.\n// Time: O(n), Space: O(charset).\nint longestUniqueSubstring(const string& s) {\n    // TODO: grow a window; when a duplicate enters, shrink from the left.\n    //       track last-seen index of each char.\n    (void)s;\n    return 0;\n}\n\nint main() {\n    cout << \"maxSumWindow -> \" << maxSumWindow({2, 1, 5, 1, 3, 2}, 3) << \" (expect 9)\\n\";\n    cout << \"longestUnique -> \" << longestUniqueSubstring(\"abcabcbb\") << \" (expect 3)\\n\";\n    return 0;\n}\n"}, {"path": "arrays/two_pointers.cpp", "folder": "arrays", "folderLabel": "Arrays", "name": "two_pointers.cpp", "desc": "Two Pointers — Phase 2 (Arrays & Strings)", "code": "// Two Pointers — Phase 2 (Arrays & Strings)\n// Compile: g++ -std=c++17 -Wall two_pointers.cpp -o /tmp/out && /tmp/out\n#include <bits/stdc++.h>\nusing namespace std;\n\n// Given a SORTED array, return indices (0-based) of two numbers adding to target.\n// Return {-1,-1} if none. Time: O(n), Space: O(1).\npair<int,int> twoSumSorted(const vector<int>& a, int target) {\n    // TODO: use a left pointer at 0 and right pointer at a.size()-1.\n    //       move inward based on whether sum < / > / == target.\n    (void)a; (void)target;\n    return {-1, -1};\n}\n\n// Reverse the array in place. Time: O(n), Space: O(1).\nvoid reverseInPlace(vector<int>& a) {\n    // TODO: swap ends moving toward the middle.\n    (void)a;\n}\n\nint main() {\n    vector<int> a = {1, 3, 4, 5, 7, 11};\n    auto [i, j] = twoSumSorted(a, 9); // expect indices of 4 and 5 -> (2,3)\n    cout << \"twoSumSorted -> (\" << i << \",\" << j << \")\\n\";\n\n    vector<int> b = {1, 2, 3, 4, 5};\n    reverseInPlace(b);\n    cout << \"reversed -> \";\n    for (int x : b) cout << x << ' ';\n    cout << \"\\n\";\n    return 0;\n}\n"}, {"path": "linked_list/singly_linked_list.cpp", "folder": "linked_list", "folderLabel": "Linked Lists", "name": "singly_linked_list.cpp", "desc": "Singly Linked List — Phase 2", "code": "// Singly Linked List — Phase 2\n// Compile: g++ -std=c++17 -Wall singly_linked_list.cpp -o /tmp/out && /tmp/out\n#include <bits/stdc++.h>\nusing namespace std;\n\nstruct Node {\n    int val;\n    Node* next;\n    Node(int v) : val(v), next(nullptr) {}\n};\n\n// Push to front. Time: O(1).\nNode* pushFront(Node* head, int v) {\n    // TODO: create a node pointing at head, return it as the new head.\n    (void)head; (void)v;\n    return head;\n}\n\n// Reverse the list iteratively. Time: O(n), Space: O(1).\nNode* reverse(Node* head) {\n    // TODO: walk the list carrying prev/curr; flip each next pointer.\n    (void)head;\n    return head;\n}\n\n// Detect a cycle (Floyd's fast & slow pointers). Time: O(n), Space: O(1).\nbool hasCycle(Node* head) {\n    // TODO: advance slow by 1 and fast by 2; they meet iff a cycle exists.\n    (void)head;\n    return false;\n}\n\nvoid print(Node* head) {\n    for (Node* p = head; p; p = p->next) cout << p->val << \" -> \";\n    cout << \"null\\n\";\n}\n\nint main() {\n    Node* head = nullptr;\n    for (int v : {3, 2, 1}) head = pushFront(head, v); // expect 1 -> 2 -> 3\n    print(head);\n    head = reverse(head);\n    print(head); // expect 3 -> 2 -> 1\n    cout << \"hasCycle -> \" << boolalpha << hasCycle(head) << \" (expect false)\\n\";\n    return 0;\n}\n"}, {"path": "stack_queue/stack.cpp", "folder": "stack_queue", "folderLabel": "Stacks & Queues", "name": "stack.cpp", "desc": "Stack (array-based) + application — Phase 2", "code": "// Stack (array-based) + application — Phase 2\n// Compile: g++ -std=c++17 -Wall stack.cpp -o /tmp/out && /tmp/out\n#include <bits/stdc++.h>\nusing namespace std;\n\n// A minimal dynamic-array stack. All ops amortized O(1).\nstruct Stack {\n    vector<int> data;\n\n    void push(int x) { /* TODO */ (void)x; }\n    void pop()       { /* TODO: guard against empty */ }\n    int  top() const { /* TODO */ return -1; }\n    bool empty() const { /* TODO */ return true; }\n    int  size()  const { /* TODO */ return 0; }\n};\n\n// Application: are the brackets balanced?  \"()[]{}\" -> true, \"(]\" -> false.\n// Time: O(n), Space: O(n).\nbool balancedParens(const string& s) {\n    // TODO: push openers; on a closer, the stack top must be its match.\n    (void)s;\n    return false;\n}\n\nint main() {\n    Stack st;\n    st.push(10); st.push(20);\n    cout << \"top -> \" << st.top() << \" (expect 20)\\n\";\n    st.pop();\n    cout << \"top -> \" << st.top() << \" (expect 10)\\n\";\n    cout << \"balanced '([{}])' -> \" << boolalpha << balancedParens(\"([{}])\") << \" (expect true)\\n\";\n    cout << \"balanced '(]'     -> \" << balancedParens(\"(]\") << \" (expect false)\\n\";\n    return 0;\n}\n"}, {"path": "searching/binary_search.cpp", "folder": "searching", "folderLabel": "Searching", "name": "binary_search.cpp", "desc": "Binary Search + variants — Phase 3", "code": "// Binary Search + variants — Phase 3\n// Compile: g++ -std=c++17 -Wall binary_search.cpp -o /tmp/out && /tmp/out\n#include <bits/stdc++.h>\nusing namespace std;\n\n// Classic: index of target in a sorted array, or -1. Time: O(log n).\nint search(const vector<int>& a, int target) {\n    // TODO: maintain [lo, hi]; compare a[mid] to target; halve the range.\n    (void)a; (void)target;\n    return -1;\n}\n\n// First index i with a[i] >= target (lower_bound). Returns a.size() if none.\n// This \"leftmost\" template generalizes to many binary-search-on-answer problems.\nint lowerBound(const vector<int>& a, int target) {\n    // TODO: search in [0, n]; shrink hi when a[mid] >= target, else lo = mid+1.\n    (void)a; (void)target;\n    return (int)a.size();\n}\n\nint main() {\n    vector<int> a = {1, 3, 3, 5, 7, 9};\n    cout << \"search(5)     -> \" << search(a, 5)     << \" (expect 3)\\n\";\n    cout << \"search(4)     -> \" << search(a, 4)     << \" (expect -1)\\n\";\n    cout << \"lowerBound(3) -> \" << lowerBound(a, 3) << \" (expect 1)\\n\";\n    cout << \"lowerBound(6) -> \" << lowerBound(a, 6) << \" (expect 4)\\n\";\n    return 0;\n}\n"}, {"path": "sorting/sorts.cpp", "folder": "sorting", "folderLabel": "Sorting", "name": "sorts.cpp", "desc": "Sorting algorithms — Phase 3", "code": "// Sorting algorithms — Phase 3\n// Compile: g++ -std=c++17 -Wall sorts.cpp -o /tmp/out && /tmp/out\n#include <bits/stdc++.h>\nusing namespace std;\n\n// Merge sort. Time: O(n log n), Space: O(n), stable.\nvoid mergeSort(vector<int>& a) {\n    // TODO: split in half, recurse, then merge the two sorted halves.\n    (void)a;\n}\n\n// Quick sort. Time: O(n log n) avg / O(n^2) worst, Space: O(log n), not stable.\nvoid quickSort(vector<int>& a, int lo, int hi) {\n    // TODO: pick a pivot, partition around it, recurse on both sides.\n    (void)a; (void)lo; (void)hi;\n}\n\nbool isSorted(const vector<int>& a) {\n    return is_sorted(a.begin(), a.end());\n}\n\nint main() {\n    vector<int> a = {5, 2, 9, 1, 5, 6};\n    mergeSort(a);\n    cout << \"mergeSort sorted? \" << boolalpha << isSorted(a) << \"\\n\";\n\n    vector<int> b = {5, 2, 9, 1, 5, 6};\n    quickSort(b, 0, (int)b.size() - 1);\n    cout << \"quickSort sorted? \" << isSorted(b) << \"\\n\";\n    return 0;\n}\n"}, {"path": "recursion/backtracking.cpp", "folder": "recursion", "folderLabel": "Recursion", "name": "backtracking.cpp", "desc": "Backtracking — Phase 3", "code": "// Backtracking — Phase 3\n// Compile: g++ -std=c++17 -Wall backtracking.cpp -o /tmp/out && /tmp/out\n#include <bits/stdc++.h>\nusing namespace std;\n\n// All subsets (power set) of nums. Time: O(n * 2^n).\nvector<vector<int>> subsets(const vector<int>& nums) {\n    vector<vector<int>> out;\n    vector<int> path;\n    // TODO: define a recursive helper(start): record path, then for each\n    //       i >= start, choose nums[i], recurse with start=i+1, un-choose.\n    (void)nums; (void)path;\n    return out;\n}\n\n// All permutations of nums. Time: O(n * n!).\nvector<vector<int>> permutations(vector<int>& nums) {\n    vector<vector<int>> out;\n    // TODO: swap-based or used[] approach; fix one position at a time.\n    (void)nums;\n    return out;\n}\n\nint main() {\n    vector<int> a = {1, 2, 3};\n    cout << \"subsets count      -> \" << subsets(a).size()      << \" (expect 8)\\n\";\n    cout << \"permutations count -> \" << permutations(a).size() << \" (expect 6)\\n\";\n    return 0;\n}\n"}, {"path": "trees/bst.cpp", "folder": "trees", "folderLabel": "Trees", "name": "bst.cpp", "desc": "Binary Search Tree — Phase 4", "code": "// Binary Search Tree — Phase 4\n// Compile: g++ -std=c++17 -Wall bst.cpp -o /tmp/out && /tmp/out\n#include <bits/stdc++.h>\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left, *right;\n    TreeNode(int v) : val(v), left(nullptr), right(nullptr) {}\n};\n\n// Insert v, returning the (possibly new) subtree root. Avg O(log n), worst O(n).\nTreeNode* insert(TreeNode* root, int v) {\n    // TODO: if null, make a node; else recurse left/right by comparison.\n    (void)root; (void)v;\n    return root;\n}\n\n// Does the tree contain v?  Avg O(log n).\nbool contains(TreeNode* root, int v) {\n    // TODO: walk down comparing v to root->val.\n    (void)root; (void)v;\n    return false;\n}\n\n// In-order traversal fills `out` in sorted order. O(n).\nvoid inorder(TreeNode* root, vector<int>& out) {\n    // TODO: left, node, right.\n    (void)root; (void)out;\n}\n\nint main() {\n    TreeNode* root = nullptr;\n    for (int v : {5, 3, 8, 1, 4, 7, 9}) root = insert(root, v);\n    cout << \"contains(4) -> \" << boolalpha << contains(root, 4) << \" (expect true)\\n\";\n    cout << \"contains(6) -> \" << contains(root, 6) << \" (expect false)\\n\";\n    vector<int> v; inorder(root, v);\n    cout << \"inorder -> \";\n    for (int x : v) cout << x << ' ';\n    cout << \"(expect 1 3 4 5 7 8 9)\\n\";\n    return 0;\n}\n"}, {"path": "trees/traversals.cpp", "folder": "trees", "folderLabel": "Trees", "name": "traversals.cpp", "desc": "Binary Tree Traversals — Phase 4", "code": "// Binary Tree Traversals — Phase 4\n// Compile: g++ -std=c++17 -Wall traversals.cpp -o /tmp/out && /tmp/out\n#include <bits/stdc++.h>\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left, *right;\n    TreeNode(int v) : val(v), left(nullptr), right(nullptr) {}\n};\n\n// Level-order (BFS) traversal, one inner vector per level. O(n).\nvector<vector<int>> levelOrder(TreeNode* root) {\n    vector<vector<int>> out;\n    // TODO: use a queue; process the current level's size before descending.\n    (void)root;\n    return out;\n}\n\n// Iterative pre-order using an explicit stack. O(n).\nvector<int> preorderIterative(TreeNode* root) {\n    vector<int> out;\n    // TODO: push root; pop-visit, then push right child then left child.\n    (void)root;\n    return out;\n}\n\nint main() {\n    // Tree:      1\n    //           / \\\n    //          2   3\n    //         / \\\n    //        4   5\n    TreeNode* root = new TreeNode(1);\n    root->left = new TreeNode(2);\n    root->right = new TreeNode(3);\n    root->left->left = new TreeNode(4);\n    root->left->right = new TreeNode(5);\n\n    cout << \"levels -> \" << levelOrder(root).size() << \" (expect 3)\\n\";\n    cout << \"preorder -> \";\n    for (int x : preorderIterative(root)) cout << x << ' ';\n    cout << \"(expect 1 2 4 5 3)\\n\";\n    return 0;\n}\n"}, {"path": "heap/min_heap.cpp", "folder": "heap", "folderLabel": "Heaps", "name": "min_heap.cpp", "desc": "Min-Heap (binary heap) — Phase 4", "code": "// Min-Heap (binary heap) — Phase 4\n// Compile: g++ -std=c++17 -Wall min_heap.cpp -o /tmp/out && /tmp/out\n#include <bits/stdc++.h>\nusing namespace std;\n\n// Array-backed binary min-heap. For node i: parent=(i-1)/2, kids=2i+1, 2i+2.\nstruct MinHeap {\n    vector<int> h;\n\n    // push: O(log n) — append then sift up.\n    void push(int x) { /* TODO */ (void)x; }\n\n    // pop the minimum: O(log n) — swap root with last, drop it, sift down.\n    void pop() { /* TODO */ }\n\n    // peek the minimum: O(1).\n    int top() const { /* TODO */ return INT_MIN; }\n\n    bool empty() const { return h.empty(); }\n\n    // helpers you'll likely want:\n    // void siftUp(int i);\n    // void siftDown(int i);\n};\n\nint main() {\n    MinHeap heap;\n    for (int x : {5, 1, 8, 3, 2}) heap.push(x);\n    cout << \"drained -> \";\n    while (!heap.empty()) { cout << heap.top() << ' '; heap.pop(); }\n    cout << \"(expect 1 2 3 5 8)\\n\";\n    return 0;\n}\n"}, {"path": "trie/trie.cpp", "folder": "trie", "folderLabel": "Tries", "name": "trie.cpp", "desc": "Trie (prefix tree) — Phase 4", "code": "// Trie (prefix tree) — Phase 4\n// Compile: g++ -std=c++17 -Wall trie.cpp -o /tmp/out && /tmp/out\n#include <bits/stdc++.h>\nusing namespace std;\n\nstruct Trie {\n    struct TNode {\n        array<TNode*, 26> kids{}; // value-initialized to nullptr\n        bool end = false;\n    };\n    TNode* root = new TNode();\n\n    // Insert a lowercase word. Time: O(len).\n    void insert(const string& word) {\n        // TODO: walk/create a child per char; mark the last node as end.\n        (void)word;\n    }\n\n    // Is `word` a complete inserted word? Time: O(len).\n    bool search(const string& word) {\n        // TODO: walk; return false if a link is missing; return node->end.\n        (void)word;\n        return false;\n    }\n\n    // Does any inserted word start with `prefix`? Time: O(len).\n    bool startsWith(const string& prefix) {\n        // TODO: same walk as search but ignore the `end` flag.\n        (void)prefix;\n        return false;\n    }\n};\n\nint main() {\n    Trie t;\n    t.insert(\"apple\");\n    cout << \"search('apple')     -> \" << boolalpha << t.search(\"apple\")     << \" (expect true)\\n\";\n    cout << \"search('app')       -> \" << t.search(\"app\")                    << \" (expect false)\\n\";\n    cout << \"startsWith('app')   -> \" << t.startsWith(\"app\")                << \" (expect true)\\n\";\n    return 0;\n}\n"}, {"path": "graph/dijkstra.cpp", "folder": "graph", "folderLabel": "Graphs", "name": "dijkstra.cpp", "desc": "Dijkstra's shortest paths — Phase 5", "code": "// Dijkstra's shortest paths — Phase 5\n// Compile: g++ -std=c++17 -Wall dijkstra.cpp -o /tmp/out && /tmp/out\n#include <bits/stdc++.h>\nusing namespace std;\n\n// Weighted graph as adjacency list of (neighbor, weight). Non-negative weights.\n// Returns shortest distance from src to every node (INF if unreachable).\n// Time: O((V + E) log V) with a binary heap.\nvector<long long> dijkstra(int n, const vector<vector<pair<int,int>>>& adj, int src) {\n    const long long INF = LLONG_MAX;\n    vector<long long> dist(n, INF);\n    // TODO: min-priority_queue of (dist, node). Pop the closest, relax its edges,\n    //       skip stale entries (popped dist > dist[node]).\n    (void)adj; (void)src;\n    return dist;\n}\n\nint main() {\n    // 0 --1--> 1 --2--> 2 ;  0 --4--> 2\n    int n = 3;\n    vector<vector<pair<int,int>>> adj(n);\n    adj[0] = {{1, 1}, {2, 4}};\n    adj[1] = {{2, 2}};\n    auto d = dijkstra(n, adj, 0);\n    cout << \"dist -> \";\n    for (long long x : d) cout << (x == LLONG_MAX ? -1 : x) << ' ';\n    cout << \"(expect 0 1 3)\\n\";\n    return 0;\n}\n"}, {"path": "graph/graph.cpp", "folder": "graph", "folderLabel": "Graphs", "name": "graph.cpp", "desc": "Graph (adjacency list) + BFS/DFS — Phase 5", "code": "// Graph (adjacency list) + BFS/DFS — Phase 5\n// Compile: g++ -std=c++17 -Wall graph.cpp -o /tmp/out && /tmp/out\n#include <bits/stdc++.h>\nusing namespace std;\n\nstruct Graph {\n    int n;\n    vector<vector<int>> adj;\n    explicit Graph(int n) : n(n), adj(n) {}\n\n    void addEdge(int u, int v, bool directed = false) {\n        adj[u].push_back(v);\n        if (!directed) adj[v].push_back(u);\n    }\n\n    // BFS order from src. Time: O(V + E).\n    vector<int> bfs(int src) {\n        vector<int> order;\n        // TODO: queue + visited[]; enqueue src, expand neighbors level by level.\n        (void)src;\n        return order;\n    }\n\n    // DFS order from src (recursive). Time: O(V + E).\n    vector<int> dfs(int src) {\n        vector<int> order;\n        vector<bool> seen(n, false);\n        // TODO: recursive helper(u): mark, record, recurse unvisited neighbors.\n        (void)src; (void)seen;\n        return order;\n    }\n};\n\nint main() {\n    Graph g(5);\n    g.addEdge(0, 1); g.addEdge(0, 2); g.addEdge(1, 3); g.addEdge(2, 4);\n    cout << \"bfs(0) size -> \" << g.bfs(0).size() << \" (expect 5)\\n\";\n    cout << \"dfs(0) size -> \" << g.dfs(0).size() << \" (expect 5)\\n\";\n    return 0;\n}\n"}, {"path": "graph/union_find.cpp", "folder": "graph", "folderLabel": "Graphs", "name": "union_find.cpp", "desc": "Union-Find / Disjoint Set Union — Phase 5", "code": "// Union-Find / Disjoint Set Union — Phase 5\n// Compile: g++ -std=c++17 -Wall union_find.cpp -o /tmp/out && /tmp/out\n#include <bits/stdc++.h>\nusing namespace std;\n\n// With path compression + union by rank, ops are ~O(alpha(n)) ≈ O(1) amortized.\nstruct DSU {\n    vector<int> parent, rank_;\n    explicit DSU(int n) : parent(n), rank_(n, 0) {\n        iota(parent.begin(), parent.end(), 0); // each node is its own root\n    }\n\n    // Find the set representative of x (with path compression).\n    int find(int x) {\n        // TODO: if parent[x] != x, set parent[x] = find(parent[x]); return parent[x].\n        (void)x;\n        return x;\n    }\n\n    // Merge the sets of a and b. Returns false if already in the same set.\n    bool unite(int a, int b) {\n        // TODO: find roots; if equal return false; attach smaller rank under larger.\n        (void)a; (void)b;\n        return false;\n    }\n\n    bool connected(int a, int b) { return find(a) == find(b); }\n};\n\nint main() {\n    DSU dsu(5);\n    dsu.unite(0, 1);\n    dsu.unite(1, 2);\n    cout << \"connected(0,2) -> \" << boolalpha << dsu.connected(0, 2) << \" (expect true)\\n\";\n    cout << \"connected(0,3) -> \" << dsu.connected(0, 3)              << \" (expect false)\\n\";\n    return 0;\n}\n"}, {"path": "dp/dp_basics.cpp", "folder": "dp", "folderLabel": "Dynamic Programming", "name": "dp_basics.cpp", "desc": "Dynamic Programming basics — Phase 6", "code": "// Dynamic Programming basics — Phase 6\n// Compile: g++ -std=c++17 -Wall dp_basics.cpp -o /tmp/out && /tmp/out\n#include <bits/stdc++.h>\nusing namespace std;\n\n// Climbing stairs: ways to reach step n taking 1 or 2 steps. O(n) time, O(1) space.\nint climbStairs(int n) {\n    // TODO: dp[i] = dp[i-1] + dp[i-2]; keep only the last two values.\n    (void)n;\n    return 0;\n}\n\n// Coin change: fewest coins to make `amount`, or -1 if impossible.\n// O(amount * coins) time, O(amount) space.\nint coinChange(const vector<int>& coins, int amount) {\n    // TODO: dp[a] = min over coins c of dp[a-c]+1; dp[0]=0, rest = INF.\n    (void)coins; (void)amount;\n    return -1;\n}\n\n// Longest common subsequence length of a and b. O(n*m) time.\nint lcs(const string& a, const string& b) {\n    // TODO: 2D table; if a[i]==b[j] -> 1+diag, else max(up, left).\n    (void)a; (void)b;\n    return 0;\n}\n\nint main() {\n    cout << \"climbStairs(5) -> \" << climbStairs(5) << \" (expect 8)\\n\";\n    cout << \"coinChange({1,2,5}, 11) -> \" << coinChange({1, 2, 5}, 11) << \" (expect 3)\\n\";\n    cout << \"lcs('abcde','ace') -> \" << lcs(\"abcde\", \"ace\") << \" (expect 3)\\n\";\n    return 0;\n}\n"}], "readme": "# Implementations\n\nImplement each data structure and algorithm from scratch here as you work through the [roadmap](../ROADMAP.md). Each file is a **skeleton with TODOs** — fill in the bodies yourself, then run the built-in `main()` to test.\n\n## Build & run\n\n```bash\ng++ -std=c++17 -Wall -Wextra -g arrays/two_pointers.cpp -o /tmp/out && /tmp/out\n```\n\nOr drop this helper in your shell:\n\n```bash\nrun() { g++ -std=c++17 -Wall -Wextra -g \"$1\" -o /tmp/dsa_out && /tmp/dsa_out; }\n# usage: run trees/bst.cpp\n```\n\n## Layout (maps to roadmap phases)\n\n| Folder | Phase | Files |\n|---|---|---|\n| `arrays/` | 2 | `two_pointers.cpp`, `sliding_window.cpp` |\n| `linked_list/` | 2 | `singly_linked_list.cpp` |\n| `stack_queue/` | 2 | `stack.cpp` |\n| `searching/` | 3 | `binary_search.cpp` |\n| `sorting/` | 3 | `sorts.cpp` |\n| `recursion/` | 3 | `backtracking.cpp` |\n| `trees/` | 4 | `bst.cpp`, `traversals.cpp` |\n| `heap/` | 4 | `min_heap.cpp` |\n| `trie/` | 4 | `trie.cpp` |\n| `graph/` | 5 | `graph.cpp`, `dijkstra.cpp`, `union_find.cpp` |\n| `dp/` | 6 | `dp_basics.cpp` |\n\n## How to use each file\n\n1. Read the complexity notes and the function signatures.\n2. Replace each `// TODO` with your implementation.\n3. Compile and run — the `main()` has small assertions/prints to check yourself.\n4. Only after it passes, compare against a reference solution.\n"};
